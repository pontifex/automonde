os: linux
dist: bionic
sudo: required
group: edge

language: php
php:
  - '8.0'

cache:
  directories:
    - vendor

branches:
  only:
    - master

services:
  - redis

before_script:
  - chmod +x bin/travis_ci_before_script.sh
  - ./bin/travis_ci_before_script.sh

script:
  - vendor/bin/phpunit
